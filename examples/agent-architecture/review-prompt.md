# Agent Architecture Review Prompt

Use the `agent-architecture` skill to review this agent workflow for reliability problems:

```text
Current workflow:
[Paste the current workflow, prompt chain, tool setup, runbook, or automation design.]

Symptoms:
[What is going wrong? Examples: inconsistent output, missed requirements, unsafe tool calls, bad handoffs, no logs.]

Consequences:
[What happens when it fails?]

Current safeguards:
[Approval gates, tests, schemas, logs, dry runs, rollback steps, or human escalation.]

Desired result:
[What should be different after the review?]
```
