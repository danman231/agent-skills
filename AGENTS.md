# Codex Instructions

Use `CLAUDE.md` as the canonical repository instruction file.

This repo is intentionally compatible with Agent Skills installers. Do not add private local machine paths to public skills or docs.
